# DFStartup
A one-click PowerShell tool for Windows that disables Fast Startup — the hybrid shutdown/hibernation mode that can cause dual-boot conflicts, driver issues, and disk state problems on some systems.
